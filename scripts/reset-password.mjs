import {loadEnv} from './env.mjs';import {ask,askPassword} from './prompt.mjs';import {openDatabase,databasePath,row,tx} from '../src/server/db.ts';import {hashPassword} from '../src/server/auth.ts';import {randomUUID} from 'node:crypto';
let db;try{loadEnv();db=openDatabase(databasePath());const email=(process.env.JR_ADMIN_EMAIL||await ask('E-mail da conta a recuperar')).toLowerCase();const user=row(db,'SELECT id FROM users WHERE email=?',email);if(!user)throw new Error('Conta não encontrada.');const password=process.env.JR_ADMIN_PASSWORD||await askPassword('Nova senha (mínimo 12 caracteres)');const hash=hashPassword(password);tx(db,()=>{db.prepare('UPDATE users SET passwordHash=? WHERE id=?').run(hash,user.id);db.prepare('DELETE FROM sessions WHERE userId=?').run(user.id);db.prepare('INSERT INTO audit(id,actorId,action,entityId,detail,createdAt) VALUES(?,?,?,?,?,?)').run(randomUUID(),user.id,'cli_password_reset',user.id,'Senha redefinida pelo operador com acesso ao servidor. Sessões revogadas.',new Date().toISOString());});console.log('Senha atualizada. As sessões antigas foram encerradas.');}catch(e){console.error(e.message);process.exitCode=1;}finally{db?.close();}

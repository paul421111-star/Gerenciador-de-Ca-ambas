# Local development. In production, use your real HTTPS origin.
APP_URL=http://localhost:3000
DATABASE_PATH=./data/jr.sqlite
BACKUP_DIR=./backups
# Passwords are created interactively with npm run setup, not stored here.
